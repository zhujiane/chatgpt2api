services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: chatgpt2api:local
    container_name: chatgpt2api
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8001}:80"
    volumes:
      - ./data:/app/data
      - ./config.json:/app/config.json
    environment:
      - TZ=Asia/Shanghai
      # 存储后端配置 (可选值: json, sqlite, postgres, git)
      - STORAGE_BACKEND=postgres

      # 数据库配置 (当 STORAGE_BACKEND=sqlite/postgres 时使用)
      - DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:5432/chatgpt2api
      # - DATABASE_URL=sqlite:///app/data/accounts.db

      # Git 仓库配置 (当 STORAGE_BACKEND=git 时使用)
      # - GIT_REPO_URL=https://github.com/user/repo.git
      # - GIT_TOKEN=ghp_xxxxxxxxxxxx
      # - GIT_BRANCH=main
      # - GIT_FILE_PATH=accounts.json

      # 认证密钥 (可选，覆盖 config.json)
      # - CHATGPT2API_AUTH_KEY=your_secret_key

      # 基础 URL (可选)
      # - CHATGPT2API_BASE_URL=https://your-domain.com

      # 注册机 Cloudflare clearance
      # 宿主机已有 FlareSolverr 时，容器内使用 host.docker.internal 访问
      - CHATGPT2API_REGISTER_CLEARANCE_MODE=${CHATGPT2API_REGISTER_CLEARANCE_MODE:-flaresolverr}
      - CHATGPT2API_REGISTER_CLEARANCE_TARGET_URL=${CHATGPT2API_REGISTER_CLEARANCE_TARGET_URL:-https://auth.openai.com}
      - CHATGPT2API_REGISTER_CLEARANCE_FLARESOLVERR_URL=${CHATGPT2API_REGISTER_CLEARANCE_FLARESOLVERR_URL:-http://host.docker.internal:8191}
      - CHATGPT2API_REGISTER_CLEARANCE_REFRESH_INTERVAL=${CHATGPT2API_REGISTER_CLEARANCE_REFRESH_INTERVAL:-600}
      - CHATGPT2API_REGISTER_CLEARANCE_TIMEOUT_SEC=${CHATGPT2API_REGISTER_CLEARANCE_TIMEOUT_SEC:-60}
